package appointment_system.domain;

/**
 * Supported appointment types in the scheduling system.
 *
 */
public enum AppointmentType {
    URGENT,
    FOLLOW_UP,
    ASSESSMENT,
    VIRTUAL,
    IN_PERSON,
    INDIVIDUAL,
    GROUP
}